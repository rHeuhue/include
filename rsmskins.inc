#if defined _rsmskins_included
	#endinput
#endif

#define _rsmskins_included

/*
 * Called when a skin is selected.
 *
 * @param index     Client's index.
 * @param skin      Skin's name.
 * @noreturn
 *
 */
forward rsmskns_skin_selected(index, skin[])